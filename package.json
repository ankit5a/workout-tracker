{
  "name": "workout-tracker-app",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack workout tracker with Express backend and Angular Tailwind frontend",
  "scripts": {
    "backend:install": "npm install --prefix backend",
    "backend:dev": "npm run dev --prefix backend",
    "backend:start": "npm start --prefix backend",
    "backend:seed": "npm run seed --prefix backend",
    "frontend:install": "npm install --prefix frontend",
    "frontend:start": "npm start --prefix frontend",
    "frontend:build": "npm run build --prefix frontend"
  }
}
